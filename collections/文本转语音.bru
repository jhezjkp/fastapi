meta {
  name: 文本转语音
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/v1/audio/speech
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{apiToken}}
}

body:json {
  {
    "input": "Morning sir! Waiting for your command.",
    "speed": 1,
    "model": "Melo TTS"
  }
}

assert {
  res.status: eq 200
  res.body.data[0].url: isString
}
